process {

    withName: '.*:CALL_REPEAT_EXPANSIONS_STRAGLR:.*' {
        publishDir = [
            enabled: false,
        ]
    }

    withName: '.*:CALL_REPEAT_EXPANSIONS_STRAGLR:ADD_FOUND_IN_TAG' {
        ext.prefix = { "${meta.id}_straglr" }
        ext.args2 = [
            '--output-type z',
            '--write-index=tbi',
            '--no-version'
        ].join(' ')
        publishDir = [
            path: { "${params.outdir}/repeats/sample/${meta.id}"},
            mode: params.publish_dir_mode,
            saveAs : { filename -> filename.equals('versions.yml') || !params.skip_repeat_annotation ? null : filename }
        ]
    }

    withName: '.*:CALL_REPEAT_EXPANSIONS_STRAGLR:BCFTOOLS_MERGE' {
        ext.prefix = { "${meta.id}_repeat_expansions" }
        ext.args = [
            '--write-index=tbi',
            '--output-type z',
            '--force-single'
        ].join(' ')
        publishDir = [
            path: { "${params.outdir}/repeats/family/${meta.id}"},
            mode: params.publish_dir_mode,
            saveAs : { filename -> filename.equals('versions.yml') || !params.skip_repeat_annotation ? null : filename }
        ]
    }
}
